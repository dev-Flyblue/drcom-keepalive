[package]
name = "drcom-keepalive"
version = "1.0.0"
edition = "2021"
description = "Drcom 校园网认证保活工具 — 掉线自动重登 + 时段控制"

[dependencies]
reqwest = { version = "0.12", features = ["blocking"] }
serde_json = "1"
chrono = "0.4"
ctrlc = "3"

[profile.release]
opt-level = "z"
lto = true
strip = true
